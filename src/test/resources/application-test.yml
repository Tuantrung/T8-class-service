spring:
  datasource:
    url: jdbc:tc:postgresql:15:///classservice_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    secret: test-secret-key-32-characters-min!
    expiry-hours: 1
